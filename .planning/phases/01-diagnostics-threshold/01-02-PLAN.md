---
id: 01-002-cli-diagnostics-threshold
phase: 1
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/docguard/cli.py
  - tests/test_cli.py
requirements: [DIAG-01, DIAG-02, DIAG-03]
requirements_addressed: [DIAG-01, DIAG-02, DIAG-03]
autonomous: true

must_haves:
  truths:
    - "When source files exist but zero endpoints are detected, docguard check exits 2 with a human-readable stderr WARNING (per DIAG-02)"
    - "An empty source directory does NOT trigger the zero-endpoints guard (short-circuit on falsy source_files)"
    - "severity_threshold filters which diffs affect the exit code — sub-threshold-only runs exit 0 while still displaying all diffs (per DIAG-03)"
    - "JSON output includes a source_warnings field populated from the parser (per DIAG-01 wiring)"
    - "Source file collection is deterministic across runs via sorted() in _collect_source_files"
  artifacts:
    - path: "src/docguard/cli.py"
      provides: "sorted() determinism fix, source_warnings propagation to metadata, zero-endpoints guard, severity_threshold exit-code filter"
      contains: "severity_threshold"
    - path: "tests/test_cli.py"
      provides: "Tests for zero-endpoints exit 2, empty-dir non-trigger, threshold exit 0, source_warnings in JSON"
      contains: "test_zero_endpoints_exits_two"
  key_links:
    - from: "src/docguard/cli.py"
      to: "parser.source_warnings"
      via: "metadata.source_warnings = parser.source_warnings after extract_endpoints"
      pattern: "source_warnings"
    - from: "src/docguard/cli.py"
      to: "cfg.check.severity_threshold"
      via: "_SEVERITY_ORDER threshold filter in exit-code block"
      pattern: "severity_threshold"
    - from: "src/docguard/cli.py"
      to: "typer.Exit(2)"
      via: "zero-endpoints guard"
      pattern: "typer.Exit\\(2\\)"
---

<objective>
Wire the three CLI-layer diagnostics into `check()` (DIAG-01 propagation, DIAG-02 zero-endpoints guard, DIAG-03 severity_threshold filter) plus a one-line determinism fix. `cli.py` is the composition root that sees both the parser's `source_warnings` and the `code_endpoints`/`spec` counts. Today: `severity_threshold` (config.py:13) is parsed but never read; zero endpoints exits 0 silently; `_collect_source_files` uses unsorted `rglob`. This plan connects already-defined data structures through the code path that currently bypasses them — pure plumbing, no new abstractions.

Purpose: Complete the user-visible diagnostics. After this plan a developer gets a loud non-zero exit on misconfiguration (zero endpoints), gets `severity_threshold` honored as documented, and sees skipped files in JSON output. Depends on plan 01-01 because it consumes `parser.source_warnings` and `DriftReportMetadata.source_warnings`.

Output: four edits to `cli.py` (sorted() fix, metadata propagation, zero-endpoints guard, threshold filter with updated import) and new CLI tests covering all three DIAG behaviors.
</objective>

## Phase Goal

**As a** developer running DocGuard in CI, **I want to** get a loud non-zero exit on misconfiguration and have severity_threshold actually filter my exit code, **so that** silent failures and ignored config keys stop producing falsely-clean runs.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-diagnostics-threshold/01-RESEARCH.md
@.planning/phases/01-diagnostics-threshold/01-PATTERNS.md
@.planning/phases/01-diagnostics-threshold/01-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts This Plan Produces

- `_collect_source_files`: `sorted(source_dir.rglob("*.py"))` — determinism fix at `cli.py:40`
- `metadata.source_warnings = parser.source_warnings` — propagation in `check()` after metadata is built
- Zero-endpoints guard in `check()`: `if source_files and not code_endpoints:` → stderr WARNING (`{n} source file(s) scanned but zero endpoints detected...`) + `raise typer.Exit(2)`
- `_SEVERITY_ORDER: dict[str, int] = {"info": 0, "warning": 1, "error": 2}` — module/local severity ranking
- Severity-threshold-filtered `has_errors`/`has_drift` in the exit-code block, gated by `cfg.check.severity_threshold` (display unchanged)
- Updated import: `from docguard.core.models import DriftReportMetadata, EndpointStatus, FieldDiff, Severity`
- Tests: `test_zero_endpoints_exits_two`, `test_empty_source_dir_does_not_trigger_zero_endpoints_guard`, `test_severity_threshold_info_does_not_block_exit_zero`, `test_source_warnings_in_json_output`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Determinism fix + source_warnings propagation + zero-endpoints guard</name>
  <files>src/docguard/cli.py, tests/test_cli.py</files>
  <read_first>
    - src/docguard/cli.py (the file being modified — read lines 1-45 for imports + _collect_source_files at line 40, and lines 140-196 for the check() body: framework guard at 145-148, extract_endpoints at 152, metadata construction at 160-166, exit-code block at 180-195)
    - tests/test_cli.py (the file being modified — read the global `runner = CliRunner()` at line 12, FIXTURES_DIR, and the TestCheckCommand class structure at lines 30-69)
    - .planning/phases/01-diagnostics-threshold/01-PATTERNS.md (cli.py Changes 1, 2, 3 — exact insertion points and stderr/Exit idioms)
    - .planning/phases/01-diagnostics-threshold/01-01-SUMMARY.md (confirm parser.source_warnings and metadata.source_warnings exist)
  </read_first>
  <behavior>
    - test_zero_endpoints_exits_two: a source dir with a non-API `.py` file (`x = 1`) plus `requirements.txt` containing `fastapi` → exit_code == 2, stderr contains "WARNING" and "zero endpoints" (case-insensitive)
    - test_empty_source_dir_does_not_trigger_zero_endpoints_guard: an empty source dir (only requirements.txt) → exit_code != 2 (guard short-circuits on falsy source_files)
    - test_source_warnings_in_json_output: a source containing a SyntaxError file run with --format json → JSON output parses and includes a "source_warnings" list naming the bad file
  </behavior>
  <action>
    Apply three edits to `cli.py`. (1) Determinism: at `_collect_source_files` line 40, wrap the iterable in `sorted(...)` → `for py_file in sorted(source_dir.rglob("*.py")):` so file order is reproducible. (2) DIAG-01 propagation: immediately after `code_endpoints = parser.extract_endpoints(source_files)` (line 152), capture `parser.source_warnings`; after the `metadata = DriftReportMetadata(...)` construction (lines 160-166), assign `metadata.source_warnings = parser.source_warnings`. Do NOT add source_warnings as a constructor kwarg — assign after construction to keep the existing constructor call unchanged. (3) DIAG-02 guard: insert immediately after line 152 (before `load_spec`) a guard `if source_files and not code_endpoints:` that prints the zero-endpoints WARNING (`f"WARNING: {len(source_files)} source file(s) scanned but zero endpoints detected. Check your 'source' directory setting and framework detection."`) via `print(..., file=sys.stderr)` and raises `typer.Exit(2)`. Use stderr + Exit(2) per the convention (Exit 1 = drift, Exit 2 = config/runtime error); do NOT use `console.print` (Rich → stdout). The `source_files and ...` short-circuit ensures an empty source dir never fires the guard. Add three new tests to `tests/test_cli.py`. For the zero-endpoints test, create a LOCAL `_stderr_runner = CliRunner(mix_stderr=False)` to assert on `result.stderr` — do NOT modify the global `runner` (existing tests depend on `mix_stderr=True`). For the JSON source_warnings test, write a SyntaxError `.py` file into tmp_path alongside a valid fixture so the run does not hit the zero-endpoints guard, invoke with `--format json` using the global `runner`, and parse the JSON to assert the bad file appears under `source_warnings`. Follow the existing TestCheckCommand fixture-copy pattern (copy fixtures to tmp_path, write `requirements.txt` with `fastapi`, pass explicit `--spec`/`--source`).
  </action>
  <verify>
    <automated>cd /Users/shishirraj/DocGuard && python -m pytest tests/test_cli.py -x -q -k "zero_endpoints or empty_source_dir or source_warnings_in_json"</automated>
  </verify>
  <acceptance_criteria>
    - `test_zero_endpoints_exits_two` passes: exit_code == 2, stderr has "WARNING" and "zero endpoints"
    - `test_empty_source_dir_does_not_trigger_zero_endpoints_guard` passes: exit_code != 2
    - `test_source_warnings_in_json_output` passes: parsed JSON `source_warnings` lists the SyntaxError file
    - Running `docguard check` twice on the same source produces byte-identical file ordering (sorted)
    - `ruff check src/docguard/cli.py` and `mypy src/docguard/cli.py` pass
  </acceptance_criteria>
  <done>Source file order is deterministic, parser source_warnings flow into JSON output, and a source-files-present/zero-endpoints run exits 2 with a clear stderr warning while an empty dir does not.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: severity_threshold exit-code filter</name>
  <files>src/docguard/cli.py, tests/test_cli.py</files>
  <read_first>
    - src/docguard/cli.py (the file being modified — read the exit-code block at lines 180-195 and the import line at line 16)
    - src/docguard/config.py (confirm `severity_threshold: str = "error"` at line ~13 and the check config shape)
    - src/docguard/core/models.py (confirm Severity enum values "info"/"warning"/"error", FieldDiff and EndpointStatus exist for import)
    - tests/test_cli.py (the file being modified — TestCheckCommand pattern, how a .docguard.yaml is written into tmp_path)
    - .planning/phases/01-diagnostics-threshold/01-PATTERNS.md (cli.py Change 4 — exact replacement exit-code block and import update)
  </read_first>
  <behavior>
    - test_severity_threshold_info_does_not_block_exit_zero: a synced spec+source with a `.docguard.yaml` setting `check.fail_on: any` and `check.severity_threshold: warning` → exit_code == 0 (no diffs at/above the warning threshold)
    - Existing exit-code tests (synced → 0, drift → 1) still pass with the default `severity_threshold: error` (default behavior unchanged when threshold == error)
  </behavior>
  <action>
    Update the import at `cli.py:16` to `from docguard.core.models import DriftReportMetadata, EndpointStatus, FieldDiff, Severity` (add EndpointStatus and FieldDiff). Replace the exit-code block (lines 180-195) so that `has_errors` and `has_drift` only count diffs whose severity meets or exceeds the threshold. Define `_SEVERITY_ORDER: dict[str, int] = {"info": 0, "warning": 1, "error": 2}` and `_threshold_level = _SEVERITY_ORDER.get(cfg.check.severity_threshold, 2)` (default 2 = error, matching the config default so existing behavior is preserved). Compute `has_errors` as: any ERROR-severity diff whose `_SEVERITY_ORDER.get(d.severity.value, 0) >= _threshold_level`. Compute `has_drift` as: `report.summary.drifted > 0 and` any diff meeting the threshold. Leave `has_missing = report.summary.missing_in_spec > 0` unchanged (missing endpoints have no diffs and are unaffected by the threshold). Keep the `should_fail`/`fail_on` dispatch and `raise typer.Exit(1)` exactly as-is. CRITICAL anti-pattern to avoid: do NOT mutate `report.endpoints[*].diffs` — the DriftReport stays complete so JSON output and display show ALL diffs; only the exit-code evaluation uses the filtered view (DIAG-03 wording: "items below the threshold are shown but do not cause a non-zero exit"). Add `test_severity_threshold_info_does_not_block_exit_zero` to `tests/test_cli.py` writing a `.docguard.yaml` into tmp_path with `severity_threshold: warning`, using the global `runner`, asserting exit_code == 0 on a synced spec/source.
  </action>
  <verify>
    <automated>cd /Users/shishirraj/DocGuard && python -m pytest tests/test_cli.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_severity_threshold_info_does_not_block_exit_zero` passes: exit_code == 0
    - All pre-existing CLI exit-code tests still pass (default threshold = error preserves prior behavior)
    - Full suite passes: `python -m pytest -q` (29 pre-existing + plan 01-01 tests + plan 02 tests)
    - DriftReport is not mutated for filtering — all diffs still appear in JSON/text output
    - `ruff check src/docguard/cli.py` and `mypy src/docguard/cli.py` pass
  </acceptance_criteria>
  <done>severity_threshold filters which diffs drive the exit code (sub-threshold-only → exit 0) while all diffs remain displayed; default threshold preserves existing pass/fail behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config → CLI | `severity_threshold` / `fail_on` come from `.docguard.yaml`, validated by Pydantic config layer |
| parser → CLI → stderr | source_warnings + zero-endpoints warning text composed from filepaths enumerated within source_path |
| CLI → exit code | exit code consumed by CI; correctness of 0/1/2 is the security-relevant contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Tampering | exit-code filter logic | mitigate | Filter is read-only over report (no mutation of report.endpoints); default _threshold_level == 2 preserves prior exit semantics so a misconfigured threshold cannot silently weaken CI beyond the documented behavior |
| T-1-04 | Information Disclosure | zero-endpoints WARNING / source_warnings paths to stderr | accept | Paths derive from rglob() within source_path; no user-controlled injection per RESEARCH Security Domain |
| T-1-05 | Denial of Service / false-negative | severity_threshold set too high hides real drift | accept | Documented config behavior (DIAG-03 by design); default "error" is the strictest practical and unchanged; surfacing remains via full display |
| T-1-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — all stdlib + existing deps per RESEARCH Package Legitimacy Audit (none) |
</threat_model>

<verification>
- `python -m pytest tests/test_cli.py -x -q` — all CLI tests pass including the four new ones
- `python -m pytest -q` — full suite passes (no regression across the 29 pre-existing tests)
- Manual: `docguard check --source <dir-with-py-but-no-routes> --spec <spec>` exits 2 with a stderr "zero endpoints" warning
- Manual: a `.docguard.yaml` with `severity_threshold: warning` produces exit 0 when only sub-threshold items exist
- `ruff check src/docguard` and `mypy src/docguard` pass (strict)
</verification>

<success_criteria>
- DIAG-02: source files present + zero endpoints → non-zero exit (2) + human-readable stderr warning; empty dir does not trigger it
- DIAG-03: `severity_threshold: warning` shows sub-threshold items but exits 0; default `error` preserves prior behavior
- DIAG-01 (CLI half): `--format json` output includes a populated `source_warnings` field (Success Criterion 4 complete end-to-end)
- Determinism: `sorted()` makes source file collection reproducible across runs
- No regression: all pre-existing tests pass
</success_criteria>

<output>
Create `.planning/phases/01-diagnostics-threshold/01-02-SUMMARY.md` when done.
</output>
